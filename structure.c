// _ REPL = read-eval-print loop
// readline
// history
// editting

// _ parsing = Split input into command and arguments
// _ execution = Execute the command with arguments
// _ built-in = Built-in commands (cd, exit, etc.)
// _ redirection = Redirect input/output (>, <, >>, |)
// _ pipes = Handle pipes (|)
// _ signal = Handle signals (ctrl + C, ctrl + \, ctrl + D)
// _ here-doc = Read input until a delimiter is found (<<)
// _ environment = Handle environment variables ($VAR)
// _ wildcards = Handle wildcards (*, ?, [])
// _ double quotes = Handle double quotes ("") and single quotes ('')
